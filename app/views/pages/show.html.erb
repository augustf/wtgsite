<h1><%= @page.title %></h1>
<%= image_tag(@page.image.url(:medium), :alt => '') unless @page.image.nil? %>
<% if current_user %>
<ul>
	<li><%= link_to "Edit", edit_page_path(@page) %></li>
</ul>
<% end %>
