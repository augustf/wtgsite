<div class="cell_full">
	<div class="cell_full-top"><div class="cell_full-top-padding"><h1><small><%= link_to "Home", :controller => "frontpage" %> > </small>About Us</h1></div></div>
	<div class="cell-padding cell-padding_full">
		<div class="indent-1">
			<div id="pgHL" class="people"><div id="pgHL-people-padding"><p>Created in 2006, we build websites and a whole lot more. <%= link_to "Read more.", Page.find(1) %></p></div></div>
			<br /><h1>Who We Are</h1>
			
			<% if current_user %>
			<div class="grayround_box">
					<div class="grayround_top"><div></div></div>
					<div class="grayround_cont"><%= link_to "Create New User", :controller => 'users', :action => 'new' %></div>
					<div class="grayround_bot"><div></div></div>
			</div>
			<% end %>
			
			<p class="showhide"><a href="#" id="show-all">Show All</a> <a href="#" id="hide-all">Hide All</a></p>
			<% @users.each do |user| %>
				<h2 class="cat-head trigger"><a href="#"><span id="h2arrow" class="h2min">&nbsp;&nbsp;</span>&nbsp;<%= user.name unless user.nil? %></a></h2>
				<div class="toggle_container">
					<div class="block landingblock">
						<div style="float:left;width:420px;">
							<% if user.website and user.website != "" %><h3><%= link_to "View My Website", user.website unless user.nil? %></h3><% end %>
							<%= user.greeting unless user.nil? %>
							<br />
							<% if current_user %>
							<div class="grayround_box">
									<div class="grayround_top"><div></div></div>
									<div class="grayround_cont"><%= link_to "Edit User", { :action => 'edit', :id => user.id } %>&nbsp;&nbsp;&nbsp;<%= link_to "Delete", user, :confirm => 'Are you sure you want to delete this user?', :method => :delete %></div>
									<div class="grayround_bot"><div></div></div>
							</div>
							<% end %>
						</div>
						<div class="screenset"><div class="screenset-padding"><%= image_tag user.photo.url(:thumb) %>
			</div></div>
					</div>
				</div>
			
			<% end %>

		</div>
	</div>
</div>
